@using Zooni.Models
@model decimal?

@{
    var pesoDecimal = Model ?? 0.1M;
    var pesoDisplay = ViewBag.MascotaPesoDisplay as string;
    var pesoFormateado = PesoHelper.FormatearPeso(pesoDecimal, pesoDisplay);
}

@pesoFormateado